<div class="modular-row head">
	<div class="container">
	<div class="center-block center"><p>
		{% set image = page.media.images|first %}
    {% if image %}
        {{ image.cropResize(600,600).html('','','align-'~page.header.image_align) }}
    {% endif %}
	</p>
		<div>{{ page.content }}</div>
	</div>
	</div>

</div>
